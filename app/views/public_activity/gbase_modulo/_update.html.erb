<li class="list-group-item list-group-item-warning">
  <span class="glyphicon glyphicon-edit"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.name : 'Guest' %></strong>
  <% if a.trackable %>
    alterou módulo <%= link_to a.trackable.name, modulo_path(a.trackable) %>.
  <% else %>
    alterou o módulo <strong><%= a.name %></strong> que já foi deletado.
  <% end %>
</li>